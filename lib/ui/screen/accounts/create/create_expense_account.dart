import 'package:flutter/material.dart';

class CreateExpensesAccount extends StatelessWidget {
  const CreateExpensesAccount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
