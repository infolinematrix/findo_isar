import 'package:flutter/material.dart';

class UpdateBankAccount extends StatelessWidget {
  const UpdateBankAccount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
