import 'package:flutter/material.dart';

class UpdateExpensesAccount extends StatelessWidget {
  const UpdateExpensesAccount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
