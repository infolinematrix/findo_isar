import 'package:flutter/material.dart';

class UpdateIncomeAccount extends StatelessWidget {
  const UpdateIncomeAccount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
